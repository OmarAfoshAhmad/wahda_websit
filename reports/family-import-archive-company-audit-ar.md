# فحص ربط أرشيف العائلات بالشركات

تاريخ الفحص: 2026-07-16.

## نطاق الفحص

- قاعدة البيانات: `waad_production_snapshot_test` المعزولة.
- العملية قراءة فقط ولم تعدل المخطط أو البيانات.
- لم تُمس قاعدة الإنتاج.

## النتيجة

- إجمالي سجلات `FamilyImportArchive`: **0**.
- قابلة للربط بشركة واحدة: **0**.
- تعارض بين عدة شركات: **0**.
- سجلات مرتبطة بمستفيدين بلا شركة: **0**.
- سجلات غير مطابقة لمستفيدين: **0**.

## الاستنتاج

لا نحتاج إلى backfill تاريخي لهذا الجدول على لقطة الإنتاج الحالية. يمكن تعديل بنيته مباشرة بطريقة توسعية لدعم تكرار `family_base_card` بين شركتين، مع إلزام كل سجل جديد بـ`company_id` المستخرج من المستفيدين على الخادم.

## أداة إعادة الفحص

أضيف الأمر:

```text
npm run audit:family-archive-company
```

ويرفض العمل على أي قاعدة لا يحتوي اسمها `test` أو `testing` أو `snapshot`.

## التنفيذ المكتمل

- أضيفت الهجرة `20260716153500_scope_family_import_archive` بهوية مستقلة وقيد فريد مركب `(company_id, family_base_card)`.
- تحتوي الهجرة حاجز أمان يرفض التنفيذ إذا كان الجدول القديم غير فارغ، بدل تخمين ملكية السجلات.
- أصبحت عمليات الاستيراد وضبط الرصيد والإيقاف والحفظ والحذف والقراءة والتراجع مقيدة بالشركة.
- عُزل كذلك احتساب تسويات تجاوز الرصيد حسب `(company_id, family_base_card)`.
- طُبقت الهجرة على `wahda_db` و`waad_production_snapshot_test` فقط، ولم تُمس قاعدة الإنتاج.

## التحقق

- فحص TypeScript: ناجح.
- اختبارات صلاحيات نطاق الشركات: 4/4 ناجحة.
- اختبارات التكامل: 3/3 ناجحة.
- اختبار فعلي داخل معاملة متراجعة لرقم عائلة واحد في شركتين: ناجح؛ حذف سجل الشركة الأولى لم يؤثر على سجل الثانية.

يمكن إعادة اختبار العزل على قاعدة اختبار بالأمر:

```text
npm run test:family-archive-company
```
