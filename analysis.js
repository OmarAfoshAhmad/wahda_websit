const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const logs = await prisma.auditLog.findMany({ where: { action: 'IMPORT_TRANSACTIONS' }, orderBy: { created_at: 'desc' }, take: 1 }); const settlements = await prisma.transaction.findMany({ where: { type: 'SETTLEMENT', is_cancelled: false }, include: { beneficiary: true } }); console.log(JSON.stringify({ log: logs[0], count: settlements.length, samples: settlements.slice(0, 10).map(s => ({ card: s.beneficiary.card_number, key: s.idempotency_key, date: s.created_at })) })); await prisma.$disconnect(); } main();