const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const results = await prisma.$queryRawUnsafe(`SELECT city, batch_number, count(*) FROM "CardIssuanceRegistryAll" GROUP BY city, batch_number ORDER BY city, batch_number`); console.log(JSON.stringify(results, (key, value) => typeof value === "bigint" ? value.toString() : value, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
